using UnityEngine;
using System.Collections;

public class CubeController : MonoBehaviour {
    // Use this for initialization
    void Start () {
        //inf=this.gameObject.AddComponent<Influence>();
        //Destroy(this.gameObject,10);
	}
	
	// Update is called once per frame
	void Update () {
	    
	}
}
